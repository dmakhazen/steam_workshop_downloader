# Промт для Codex

## Проект: SteamMod Manager — статичный сайт на GitHub Pages

Создай одностраничное веб-приложение (static site, только HTML + CSS + vanilla JS, без серверной части, без фреймворков типа React/Vue). Сайт будет размещён на GitHub Pages.

---

### Цель проекта

Пользователь хочет скачивать моды для игр (в первую очередь RimWorld) через SteamCMD, минуя Steam клиент. Сайт помогает: найти моды → добавить в список → экспортировать скрипт для SteamCMD.

---

### Технические ограничения

- Только статика: `index.html`, `style.css`, `app.js` (можно разбить на модули)
- Никакого бэкенда, никаких серверных запросов с секретными ключами
- Хранение данных: `localStorage` (постоянное хранение списков и настроек)
- Steam Workshop API напрямую из браузера не доступен из-за CORS. Поэтому для отображения мастерской использовать **iframe** со ссылкой вида:
  ```
  https://steamcommunity.com/workshop/browse/?appid=294100&browsesort=trend&section=readytouseitems
  ```
  где `294100` — AppID RimWorld. Iframe встраивается в основную область просмотра.

---

### Функциональность по пунктам

#### 1. Выбор игры
- В шапке сайта — дропдаун или кнопки выбора игры
- Изначально доступны минимум: **RimWorld (AppID: 294100)**, **Stardew Valley (AppID: 413150)**, **Terraria (AppID: 105600)**
- При выборе игры меняется AppID, iframe обновляется под неё

#### 2. Просмотр мастерской через iframe
- Основная область страницы — iframe с Steam Workshop
- URL iframe строится динамически: `https://steamcommunity.com/workshop/browse/?appid={APPID}&browsesort={SORT}&section=readytouseitems`
- Параметр `browsesort` управляется кнопками/дропдауном на сайте: `trend` (популярное), `totaluniquesubscribers` (всего подписчиков), `lastupdated` (недавно обновлено), `textsearch` (поиск по названию)
- При выборе "поиск по названию" — показывать текстовое поле, строить URL с `&searchtext={query}`
- Фильтры (теги, период) передаются как параметры в URL iframe

#### 3. Добавление мода в список
- В интерфейсе сайта (над или рядом с iframe) есть поле ввода: **"Вставь ссылку на мод"** и кнопка **"Добавить в активный список"**
- Пример ссылки: `https://steamcommunity.com/sharedfiles/filedetails/?id=2009463077`
- Из ссылки парсится `id` (Workshop Item ID)
- Добавленный мод отображается в сайдбаре как карточка: название (если введено вручную) или "Мод #ID", Workshop ID, кнопка удалить из списка
- Опционально: рядом с полем ввода ссылки — поле для имени мода (необязательное)

#### 4. Управление списками
- В сайдбаре или отдельной панели — раздел "Мои списки"
- Кнопка **"Создать список"** → запрашивает имя через `prompt()`
- Список списков отображается: каждый список кликабельный → при клике становится "активным"
- Активный список выделен визуально
- Рядом с каждым списком — кнопка удалить список
- Все списки хранятся в `localStorage` под ключом `steammod_lists`

#### 5. Экспорт и импорт списков
- Кнопка **"Экспортировать список"** → скачивает JSON файл с именем `{название_списка}.json`
  ```json
  {
    "name": "Мой список модов",
    "appid": "294100",
    "mods": [
      { "id": "2009463077", "name": "Combat Extended" },
      { "id": "818773962", "name": "Rimfactory" }
    ]
  }
  ```
- Кнопка **"Импортировать список"** → `<input type="file">`, читает JSON, добавляет список в localStorage

#### 6. Генерация скрипта SteamCMD
- Кнопка **"Скачать скрипт SteamCMD"** генерирует `.bat` файл (Windows) и `.sh` файл (Linux) одновременно или по выбору
- Имя папки для скачивания = название активного списка (пробелы → подчёркивания)
- Формат `.bat` скрипта:
  ```bat
  @echo off
  set LIST_NAME=Мой_список_модов
  steamcmd.exe ^
    +login anonymous ^
    +force_install_dir "C:\SteamCMD\mods\%LIST_NAME%" ^
    +workshop_download_item 294100 2009463077 ^
    +workshop_download_item 294100 818773962 ^
    +quit
  ```
- Формат `.sh` скрипта:
  ```bash
  #!/bin/bash
  LIST_NAME="Мой_список_модов"
  ./steamcmd.sh \
    +login anonymous \
    +force_install_dir "$HOME/steamcmd/mods/$LIST_NAME" \
    +workshop_download_item 294100 2009463077 \
    +workshop_download_item 294100 818773962 \
    +quit
  ```
- Файлы скачиваются через `Blob` + динамический `<a>` с `download` атрибутом

#### 7. Альтернативные сайты для скачивания модов
- Кнопка **"Скачать через другие сайты"** открывает модальное окно или выпадающий блок со списком сервисов
- Для каждого мода из активного списка генерировать ссылки на:
  - **SteamRIP**: `https://steamrip.com/` (общая ссылка, т.к. прямые ссылки нестабильны — открывать сайт)
  - **WorkshopDL**: `https://workshopdl.net/` — открывать сайт
  - **gg.deals Steam Workshop downloader**: `https://gg.deals/` — общая
  - **Steamworkshopdownloader.io**: `https://steamworkshopdownloader.io/` — открывать сайт
  - Для каждого из рабочих сервисов, если у них есть прямой URL с mod ID, генерировать прямую ссылку, например:
    - `https://steamworkshopdownloader.io/` → открывать с параметром если поддерживается
- В модальном окне: таблица модов из списка × сервисы, в каждой ячейке — кнопка/ссылка
- Важно: все внешние ссылки открывать в `target="_blank"`

---

### Структура интерфейса

```
┌──────────────────────────────────────────────────────────┐
│  HEADER: [Логотип/Название] [Выбор игры ▼]               │
├────────────────────────┬─────────────────────────────────┤
│  SIDEBAR               │  MAIN AREA                      │
│  ─────────────         │  ─────────────                  │
│  Фильтры iframe:       │  [iframe Steam Workshop]        │
│  [Сортировка ▼]        │                                 │
│  [Поиск...]            │                                 │
│  ─────────────         │                                 │
│  Мои списки:           │                                 │
│  + Создать список      │                                 │
│  > [Список 1] ✕        │                                 │
│    [Список 2] ✕        │                                 │
│  ─────────────         │                                 │
│  Активный список:      │                                 │
│  "Список 1"            │                                 │
│  [Вставь ссылку..] [+] │                                 │
│  • Мод 1 [✕]           │                                 │
│  • Мод 2 [✕]           │                                 │
│  ─────────────         │                                 │
│  [⬇ .bat] [⬇ .sh]     │                                 │
│  [⬇ JSON] [⬆ JSON]    │                                 │
│  [🌐 Другие сайты]     │                                 │
└────────────────────────┴─────────────────────────────────┘
```

---

### Стиль оформления

- Тёмная тема (фон `#1a1a2e`, акцент `#e94560`, карточки `#16213e`)
- Шрифт: системный или Google Fonts `Inter`
- Адаптивность: на мобильном сайдбар скрывается/выезжает по кнопке
- CSS переменные для цветов

---

### Файловая структура на выходе

```
index.html
style.css
app.js
README.md  ← инструкция: как разместить на GitHub Pages, как установить SteamCMD
```

---

### Важные детали реализации

1. `localStorage` ключи: `steammod_lists` (массив списков), `steammod_active_list` (ID активного), `steammod_selected_game` (AppID)
2. При первом запуске создать дефолтный список "Мой первый список" и выбрать RimWorld
3. Парсинг Workshop ID из ссылки: регулярное выражение `[?&]id=(\d+)`
4. Iframe не позволяет взаимодействовать с содержимым (cross-origin), это нормально — пользователь кликает внутри iframe вручную, а добавление происходит через поле ввода ссылки на сайдбаре

---

Напиши полный рабочий код всех файлов.
